#!/usr/local/bin/expect -f

set timeout 10

spawn dr auth setURL

# If we get an overwrite prompt be sure to say yes to it first
expect {
    "do you want to overwrite" {
        send -- "y\r\n"
    }
    timeout
}

expect "please enter the URL you use"
send -- "https://testing.example.com\r\n"

expect eof
